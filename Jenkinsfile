@Library('roboshop') _

roboshop (
        COMPONENT             : 'frontend1',
        PROJECT_NAME          : "RoboShop",
        SLAVE_LABEL           : "MASTER",
        SKIP_NEXUS_UPLOAD     : false
)
